1. run `npm install` in both website and product_list folder to install their respective packages.
2. in two terminals boot up these codebases by navigating to their directories and individually using `npm start` command.
3. view localhost:3001 for viewing our website which used product_list as MFE and itself is a host MFE.
